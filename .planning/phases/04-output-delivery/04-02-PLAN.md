---
phase: 04-output-delivery
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - tests/test_byte_fidelity.py
  - lovable-spec.md
autonomous: false
requirements: [OUTP-01, FRNT-04]

must_haves:
  truths:
    - "Byte-fidelity test scaffold exists and will catch encoding/format regressions when user provides fixture files"
    - "Lovable spec includes results/download section so frontend can be regenerated with download buttons"
    - "Results section shows summary (total, errors, skipped) and download buttons for all available files"
  artifacts:
    - path: "tests/test_byte_fidelity.py"
      provides: "Byte-level comparison tests against reference fixtures (skipped until fixtures arrive)"
      contains: "skipif"
    - path: "lovable-spec.md"
      provides: "Updated Lovable spec with results/download section"
      contains: "download"
  key_links:
    - from: "lovable-spec.md"
      to: "api/jobs.py"
      via: "GET /jobs/{id}/files metadata endpoint drives button rendering"
      pattern: "files.*metadata"
---

<objective>
Create byte-fidelity test scaffold for regression testing and update the Lovable frontend specification with the results/download section.

Purpose: The byte-fidelity tests are the quality gate for OUTP-01 (byte-perfect ISS.NET format). The Lovable spec update enables FRNT-04 (results page with download buttons). End-of-phase verification confirms everything works together.
Output: tests/test_byte_fidelity.py (skipped until user provides fixtures), updated lovable-spec.md with results section.
</objective>

<execution_context>
@C:/Users/Havai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Havai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-output-delivery/04-CONTEXT.md
@.planning/phases/04-output-delivery/04-RESEARCH.md
@.planning/phases/04-output-delivery/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From api/jobs.py (new routes):
```python
# GET /jobs/{id}/files -> JobFilesResponse
# GET /jobs/{id}/download/txt -> Response (UTF-8 TXT)
# GET /jobs/{id}/download/csv -> Response (UTF-8-BOM CSV)
# GET /jobs/{id}/download/txt/{vigencia} -> Response (UTF-8 split TXT)
```

From api/models.py (new models):
```python
class FileEntry(BaseModel):
    type: Literal["txt", "csv", "txt_split"]
    label: str
    url: str
    vigencia: str = ""

class JobSummary(BaseModel):
    total: int
    errors: int
    skipped: int
    processing_seconds: float | None = None

class JobFilesResponse(BaseModel):
    job_id: str
    emp_cod: str
    vigencia: str
    summary: JobSummary
    files: list[FileEntry]
```

From api/models.py (existing):
```python
class JobStatusResponse(BaseModel):
    result_ready: bool  # True when status=="completed" and result stored
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create byte-fidelity test scaffold and fixture directory structure</name>
  <files>tests/test_byte_fidelity.py, tests/fixtures/README.md</files>
  <action>
1. Create directory structure for test fixtures:
   - tests/fixtures/goiania/input/ (empty, user will add XML files)
   - tests/fixtures/goiania/expected/ (empty, user will add desktop-generated TXT+CSV)
   - tests/fixtures/aparecida/input/ and tests/fixtures/aparecida/expected/
   - tests/fixtures/anapolis/input/ and tests/fixtures/anapolis/expected/
   - tests/fixtures/brasilia/input/ and tests/fixtures/brasilia/expected/
   - Create a tests/fixtures/README.md explaining what files the user needs to provide:
     "Place desktop-generated reference files here for byte-fidelity testing.
      Each municipality folder needs:
        input/  — the XML files that were processed
        expected/ — the TXT and CSV files the desktop app generated
      Filename convention: {emp_cod}_{vigencia}.txt, relatorio_{emp_cod}_{vigencia}.csv"

2. Create tests/test_byte_fidelity.py with parametrized tests for all 4 municipalities:
   - Use `FIXTURE_DIR = Path(__file__).parent / "fixtures"`
   - `MUNICIPALITIES = ["goiania", "aparecida", "anapolis", "brasilia"]`
   - Each test is `@pytest.mark.skipif(not (FIXTURE_DIR / municipality / "expected").glob("*.txt"), reason="No reference fixtures for {municipality}")`
   - `test_txt_byte_fidelity_{municipality}`: reads expected TXT bytes from fixtures, compares against actual bytes. For now, the test body loads the expected file and asserts it exists and has content > 0 bytes, with a TODO comment marking where the actual API call comparison will go once fixtures arrive.
   - `test_csv_byte_fidelity_{municipality}`: same pattern for CSV files.
   - Use `@pytest.mark.parametrize("municipality", MUNICIPALITIES)` to avoid code duplication.
   - Tests should be skippable — when no fixture files exist, they skip gracefully (not fail).

3. Verify: `pytest tests/test_byte_fidelity.py -x -q` runs without errors (all tests skipped is acceptable).
  </action>
  <verify>
    <automated>cd "C:/Users/Havai/Desktop/Rest iss.net" && python -m pytest tests/test_byte_fidelity.py -x -q -v 2>&1 | tail -10</automated>
  </verify>
  <done>tests/test_byte_fidelity.py exists with parametrized tests for 4 municipalities. All tests skip gracefully when fixtures are absent. Directory structure tests/fixtures/{municipality}/{input,expected}/ exists.</done>
</task>

<task type="auto">
  <name>Task 2: Update Lovable spec with results/download section</name>
  <files>lovable-spec.md</files>
  <action>
Read the existing lovable-spec.md and add a new section (Section 12 or the next available number) for the Results/Download UI. Per user decisions in CONTEXT.md:

- Same progress page transitions to a "completed" state with download section below (no separate results page)
- Brief summary: total notes processed, errors count, skipped count, processing time
- Download buttons: main TXT, one per vigencia split, CSV report
- Re-download available until cleanup (1h TTL or next job starts)
- "New Job" button resets page to upload form

Add the following to the spec:

**Section: Results and Download (when job status = "completed")**

Component: ResultsSection (renders below ProgressSection when result_ready=true)

Data source: `GET /jobs/{id}/files` returns `JobFilesResponse` with:
- summary (total, errors, skipped, processing_seconds)
- files[] array with type, label, url for each downloadable file

Layout:
- Summary card: "Processed {total} notes in {processing_seconds}s — {errors} errors, {skipped} skipped"
- If errors > 0, show errors count in red/destructive color
- Download buttons section with consistent styling:
  - Primary button (blue): main TXT file (label from files[type="txt"])
  - Secondary button (outline): CSV report (label from files[type="csv"])
  - If files[type="txt_split"] entries exist, show a "Split Files" subsection with one outline button per split, labeled with the vigencia
- All download buttons use anchor tag with href={file.url} and download attribute (browser handles the Content-Disposition header)
- "Start New Job" button below downloads — resets the page state to the upload form

State transitions:
- ProgressSection polls GET /jobs/{id}/status
- When result_ready=true, stop polling, fetch GET /jobs/{id}/files once
- Render ResultsSection with the files response
- "Start New Job" clears job state and shows UploadForm

Color palette: use the existing spec's color tokens (primary for main action, muted for secondary).
  </action>
  <verify>
    <automated>cd "C:/Users/Havai/Desktop/Rest iss.net" && grep -c "download\|ResultsSection\|Start New Job" lovable-spec.md</automated>
  </verify>
  <done>lovable-spec.md updated with results/download section. Section includes summary display, download button layout for TXT/CSV/splits, state transition from progress to results, and "Start New Job" reset.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase verification — review backend, tests, and spec</name>
  <action>
Human verifies Phase 4 deliverables: download endpoints, test coverage, byte-fidelity scaffold, and Lovable spec update.
  </action>
  <what-built>
Phase 4 backend is complete: 4 download endpoints (main TXT, CSV, split TXT, file metadata) with full unit test coverage. Byte-fidelity test scaffold is ready for reference fixtures. Lovable spec has been updated with the results/download section.
  </what-built>
  <how-to-verify>
1. Run full test suite: `python -m pytest tests/ -x -q` — all tests should pass (byte-fidelity tests will be skipped)
2. Review the download test results: `python -m pytest tests/test_download.py -v` — all 13 tests should be green
3. Review lovable-spec.md results section — confirm it matches your vision for the download UI
4. Check tests/fixtures/ directory structure exists for all 4 municipalities
5. When ready, provide desktop-generated reference TXT+CSV files for the byte-fidelity tests:
   - Place Goiania files in tests/fixtures/goiania/expected/
   - Place Aparecida files in tests/fixtures/aparecida/expected/
   - Place Anapolis files in tests/fixtures/anapolis/expected/
   - Place Brasilia files in tests/fixtures/brasilia/expected/
  </how-to-verify>
  <verify>Human confirms all deliverables are satisfactory</verify>
  <done>Phase 4 approved by user</done>
  <resume-signal>Type "approved" to complete Phase 4, or describe any issues with the download endpoints or spec</resume-signal>
</task>

</tasks>

<verification>
- `pytest tests/ -x -q` — full suite green
- `pytest tests/test_download.py -v` — all 13 download tests pass
- `pytest tests/test_byte_fidelity.py -v` — all tests skip gracefully (no fixtures yet)
- lovable-spec.md contains results/download section
- tests/fixtures/ directory structure exists for 4 municipalities
</verification>

<success_criteria>
- Byte-fidelity test infrastructure ready to catch regressions once user provides fixtures
- Lovable spec enables frontend regeneration with download buttons
- End-of-phase human verification confirms backend + spec quality
</success_criteria>

<output>
After completion, create `.planning/phases/04-output-delivery/04-02-SUMMARY.md`
</output>
